<%- include('../../partials/header.ejs') %>

<!-- Page Wrapper -->
<div id="wrapper">
  <!-- side bar -->
  <%- include('../../partials/sidebar.ejs') %>
  <!-- endsidebar -->

  <!-- Content Wrapper -->
  <div id="content-wrapper" class="d-flex flex-column">
    <!-- Main Content -->
    <div id="content">
      <!-- navbar -->
      <%- include('../../partials/navbar.ejs') %>
      <!-- end navbar -->

      <!-- Begin Page Content -->
      <div class="container-fluid">
        <%- include('../../partials/flash_message.ejs') %><%-
        include('table_bank.ejs') %>
      </div>
      <!-- /.container-fluid -->
    </div>
    <!-- End of Main Content -->

    <!-- Footer -->
    <%- include('../../partials/footer.ejs') %>
    <!-- End of Footer -->
  </div>
  <!-- End of Content Wrapper -->
</div>
<!-- End of Page Wrapper -->

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
  <i class="fas fa-angle-up"></i>
</a>

<%- include('add_modal_bank.ejs') %> <%- include('edit_modal_bank.ejs') %><%-
include('../../partials/script.ejs') %>
<script>
  $('#dataTable').on('click', '.button-update', function () {
    const id = $(this).data('id');
    const bank = $(this).data('namebank');
    const rekening = $(this).data('nomorrekening');
    const name = $(this).data('name');
    const image = $(this).data('image');
    console.log(bank);
    $('#edit-modal-bank').modal('show');
    $('.id').val(id);
    $('.nameBank').val(bank);
    $('.nomorRekening').val(rekening);
    $('.name').val(name);
    $('.image').val(image);
  });
</script>
